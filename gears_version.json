{
    "version": "2.2.7",
    "compatible_gears": "2.3.1",
    "min_gears": "2.3.1",
    "changelog": "Source search now shows CACHED results only by default. External cache-check is on and the automatic 'rescrape without cache' pass is off, so the list is limited to instantly-playable (cached) sources instead of a mix of cached and uncached. Uncached sources are still reachable via the 'Show Uncached Only' button when you need them."
}
