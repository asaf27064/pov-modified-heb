{
    "version": "1.0.6",
    "changelog": "Drop the !String.IsEmpty(Skin.String(OSD_Timeout)) gate from Action_OSD_Button onfocus/onclick. The wizard preserves an empty user settings.xml across reinstalls, so the gate blocked the auto-close timer for everyone who didn't manually set OSD_Timeout. The $VAR[OSD_Timeout_Seconds] fallback to 5s now applies universally.",
    "skin_version": "3.x"
}
